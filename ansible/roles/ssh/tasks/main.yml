---
# tasks file for ssh

- name: SSH configuration
  template:
    src: ssh_config.j2
    dest: "/etc/ssh/ssh_config"
    owner: "root"
    group: "root"
    mode: 0644
    backup: yes
  notify:
    - Restart ssh

- name: SSHD daemon configuration
  template:
    src: sshd_config.j2
    dest: "/etc/ssh/sshd_config"
    owner: "root"
    group: "root"
    mode: 0644
    backup: yes
  notify:
    - Reload sshd systemd
